name: pagination_builder_example
description: Example application for the pagination_builder package.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  pagination_builder:
    path: ../
  http: ^0.13.5
  sqflite: ^2.2.0
  path: ^1.8.2
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
